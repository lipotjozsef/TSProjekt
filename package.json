{
  "name": "sport",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cls && concurrently --kill-others \"npx json-server --port 4512 ./src/assets/JSONData/db.json\" \"vite\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "json-server": "1.0.0-beta.15",
    "concurrently": "~9.2.1"
  },
  "dependencies": {
    "bootstrap": "~5.3.8"
  }
}
